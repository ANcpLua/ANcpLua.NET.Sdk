<Project>
  <!-- Examples use the ANcpLua.NET.Sdk for auto-configured service defaults -->
  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>
</Project>
