<Project>
  <!-- Import version info for consistent package versioning -->
  <Import Project="$(MSBuildThisFileDirectory)../common/Version.props"/>

  <PropertyGroup>
    <ANcpLuaSdkName>ANcpLua.NET.Sdk.Web</ANcpLuaSdkName>
    <_MustImportMicrosoftNETSdk Condition="'$(UsingMicrosoftNETSdk)' != 'true'">true</_MustImportMicrosoftNETSdk>

    <CustomBeforeDirectoryBuildProps>$(CustomBeforeDirectoryBuildProps);$(MSBuildThisFileDirectory)../common/Common.props</CustomBeforeDirectoryBuildProps>
    <BeforeMicrosoftNETSdkTargets>$(BeforeMicrosoftNETSdkTargets);$(MSBuildThisFileDirectory)../common/Common.targets</BeforeMicrosoftNETSdkTargets>
  </PropertyGroup>

  <!-- GlobalPackageReference must be imported BEFORE Microsoft.NET.Sdk -->
  <Import Project="$(MSBuildThisFileDirectory)../common/GlobalPackages.props"/>

  <Import Project="Sdk.props" Sdk="Microsoft.NET.Sdk.Web" Condition="'$(_MustImportMicrosoftNETSdk)' == 'true'"/>

  <!-- Enhanced Features (props phase) -->
  <Import Project="$(MSBuildThisFileDirectory)../common/ItemDefaults.props"/>
  <Import Project="$(MSBuildThisFileDirectory)../common/InternalsVisibleTo.props"/>
  <Import Project="$(MSBuildThisFileDirectory)../common/TerminalLogger.props"/>
  <Import Project="$(MSBuildThisFileDirectory)../common/BuildCheck.props"/>

  <!-- Always import Common.props (CustomBeforeDirectoryBuildProps only works with Directory.Build.props) -->
  <Import Project="$(MSBuildThisFileDirectory)../common/Common.props"/>

  <!-- Enforcement and determinism/SourceLink -->
  <Import Project="$(MSBuildThisFileDirectory)../Enforcement/Enforcement.props"/>
  <Import Project="$(MSBuildThisFileDirectory)../Enforcement/DeterminismAndSourceLink.props"/>
</Project>